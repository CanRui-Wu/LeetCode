530 Minimum Absolute Difference in BST
存放中序遍历的结果，就是从小到大的有序数组，然后取两两之间的距离对比即可。

220. Contains Duplicate III
踩了很多坑，首先是利用multiset的lowerbound，返回第一个不小于给定值的迭代器，然而这个迭代器只支持与其他迭代器比较相等，而不支持<，或是迭代器之间的加减，属于双向迭代器，而不是随机访问迭代器。
然后是整数溢出，因为题目中整数要做加减法，有可能会溢出。
根据lowerbound取到的元素值，和上界比较，如果小于等于上界，就说明有重复。
要在k范围内不断维护multiset，保证其只存在当前的k个元素。

